{
  "$schema": "https://schema.tauri.app/capability/2",
  "identifier": "default",
  "description": "Overlay and settings windows: events + app commands",
  "windows": ["overlay", "settings"],
  "permissions": ["core:default"]
}
